# Lost Luggage Service – Página de contacto

Archivos:
- `index.html` → página lista para publicar (GitHub Pages).
- `QR INT DESK.png` → imagen del código QR (debe estar en la misma carpeta que `index.html`).

Instrucciones rápidas:
1. Sube ambos archivos al repositorio (raíz).
2. Activa GitHub Pages: Settings → Pages → Branch: `main` /root.
3. Abre la URL que te genere GitHub Pages.
